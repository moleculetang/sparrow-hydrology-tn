# 20260905_2：逐日 TN 参考实现与数值修复

使用 _1 冻结的父数据。`tn_reference.py` 是独立的 NumPy/Numba 双氮库与河网实现，
不导入旧实验的运行器，不拟合水文。保留原 lifetime 的竞争额外损失定义。

先用 `validate_reference.py` 比较：
1. 独立逐日递推与旧月系数方程的本地月负荷、库存；
2. 实际逐日氮负荷与按水量重新分配月氮的时序差别；
3. 水量回放与正式水文各控制体；
4. 全源—作物—氮库—河道损失—水库—终端质量闭合。

全域输出采用与水文一致的控制体：单控制水库河段是坝后释放加旁路，
多入库支流的控制河段保留入库断面，水库释放在共同下游只注入一次。
站点观测算子单独保存坝前河段内位置，不混用坝后水量。

本阶段尚需旧结构重拟合与拟合器梯度核查，参考实现通过不等于整个阶段完成。
