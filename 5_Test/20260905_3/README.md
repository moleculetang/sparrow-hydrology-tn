# 20260905_3：训练目标 × 过程区域化

使用 _2 已验证的逐日核与显式梯度，对 GLOBAL、H7_CONTACT、H7_CONTACT_LIFETIME
分别比较 Student-t 与站点归一化 MSE。H7 只作用于接触率/lifetime 的光滑有界参数映射。

训练只使用 `primary_gate=true` 的河道观测：白盆珠位置未定和开放湖泊域不进入目标，
但保留预测与敏感性输出。F24/F25 使用全部对应年份，而不是仅2021起训。
所有候选和修复旧结构对照使用同一训练掩码。实际样本数必须随结果报告。

每折五个预先固定起点，不读取任何旧TN拟合checkpoint。只在同一输入哈希、代码哈希、
观测ID及模型配置下恢复本轮检查点。单次起点的结果不代表最终选模结论。

共用命令入口：`E:\SPARROW\5_Test\20260905_2\scripts\fit_models.py`。
该冻结入口处理时间折/全量拟合；后续结构和嵌套空间分支由 _4/_5 的独立入口执行。

## 已执行的开发矩阵结果

140次拟合（4个前向年份×5起点×7组合）已完成。5个起点出现函数变化停止但梯度仍大，
经同一起点的显式恢复全部达到投影梯度门槛。原始报告保存在 recovery_archive。
每个组合/折按训练目标选择起点；以下合并2020—2023 OOF，4546条主资格观测、118站。

| 结构与目标 | 单站NSE中位数 | 单站NSE第25百分位 | 平均站点log-RMSE |
|---|---:|---:|---:|
| 重拟合旧H7对照 | -1.5254 | -4.0892 | 0.2777 |
| GLOBAL，归一化MSE | -4.7421 | -9.8524 | 0.4125 |
| H7_CONTACT，归一化MSE | -3.0074 | -6.1838 | 0.3669 |
| H7_CONTACT_LIFETIME，归一化MSE | -3.0633 | -6.2431 | 0.3656 |
| GLOBAL，Student-t | -5.1963 | -10.3662 | 0.4105 |
| H7_CONTACT，Student-t | -3.7873 | -9.3026 | 0.3841 |
| H7_CONTACT_LIFETIME，Student-t | -3.7472 | -9.3280 | 0.3833 |

所有新基准均未通过升级条件。训练期站点均值的诊断基准（未见站使用训练站点均值的
等站平均）NSE中位数为-0.3135；这是对照，不属于本轮可接受的过程预测产品。

`oof_error_decomposition.json`进一步显示，H7_CONTACT+归一化MSE的站内时间相关
中位数为-0.0567；即使只在诊断中消去评价期平均偏差，NSE中位数仍为-1.5122。
因此时间变化本身仍有明显误差，不能靠局地平均值修正来宣称机制问题已解决。
后续继续执行 _4 水文状态依赖和时序试验，以及 _5/_6 验证与最终实验导出。
