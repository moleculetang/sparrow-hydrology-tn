# 20260905_6：确认、最终拟合与全域导出

当前状态：**本轮全部注册实验、数值验收与科学复核完成；TN精度仍未达标，不升级正式主线。**

最终结论与完整表格见 [final_report.md](E:/SPARROW/5_Test/20260905_6/final_report.md)。
`reports/scientific_review.json` 记录逐项科学复核，`reports/final_report_manifest.json`
记录最终报告与证据哈希。全轮1150个计划起点、11项剖面已经完成，数值/产物验收22/22通过。

开发选择已经冻结为 H7_CONTACT_LIFETIME + 站点归一化MSE + 两系数水文状态接触率，
保持 CENTRAL 月历/月初源项、矿质氮lifetime和慢水氮库存。
选择只依据2020—2023完整OOF单站NSE中位数；2024及2025未用于选择该候选。
冻结记录为 `reports/development_choice.json`。

该候选的开发NSE中位数为-0.9277，仍未通过>0门槛。
相对旧对照的按终端树重采样区间也跨零，因此不能以开发点估计改善宣布成功。

在 `_5` 嵌套空间验证之后，已完成：

1. 固定候选的2024回顾性确认、2025敏感性，与同折重新拟合旧对照比较。
2. 共享参数先验尺度×0.5/×2的2023和2024敏感性，不用这些结果重新选择候选。
3. F24（2016—2024）和F25（2016—2025）各5个物理起点拟合。
4. 独立过程映射/逐日核回放，核对梯度、氮质量守恒以及230个reach的完整输出。
5. F24参数边界、局部曲率和lifetime/接触指数/河道参数的剖面诊断。
6. 逐项验收并形成最终科学判断。所有输出保持EXPERIMENTAL_NOT_PROMOTED，
   不自动替换正式水文或TN主线。

完整执行控制器：`E:\SPARROW\5_Test\20260905_5\scripts\resume_program.py`。
实时阶段状态：`E:\SPARROW\5_Test\20260905_5\reports\resumed_program_queue.json`。
`reports/completion_audit.json` 已记录22/22项实际验收；随后独立核对了产物覆盖、
浓度计算、干月、质量标记、参数边界及20个拟合参数的有限差分覆盖。
F24/F25分别有176640/179400条reach月记录，均覆盖230个reach。

确认期报告已单列预注册排除域（reach17位置未定、tree163开放湖泊）的已有预测指标。
这些分组不参与训练、候选选择和主评价；缺少观测时报告0行/未定义指标，
不将空分组称作验证通过。其观测覆盖由完成审计对照冻结观测表重新核对。
该项仅补齐结果报告，不增加拟合，也不改变模型、损失或门槛。

## 年度指标的独立验收补充

`scripts/audit_temporal_evidence.py` 从实际所选起点预测独立重算2024确认、2025敏感性
和四组先验敏感性的NSE中位数、q25、log-RMSE及站内相关；核对五起点选择、
真实观测值、完整评价ID、2016至测试年前一年的训练排除，以及两个预注册排除域。
这项检查不重新拟合，不生成校正预测，也不修改拟合数学核心。
验收已纳入 `scripts/completion_audit.py`；只有六组评价全部完成且当前文件哈希匹配
才接受完整通过。执行中的部分通过不能代替最终验收。

本次最终验收为六组完整通过。科学结果为：2024/2025新模型NSE中位数
-1.9215/-1.7475，旧对照-1.7346/-1.2725；终端树空间留出也未通过非退化门槛。
F24 eta_upper在上界1且存在界外下降压力；MAP和局部剖面不能解释成完整后验。
数值闭合不代表TN精度合格，现有证据也没有证明双库存结构的理论极限。
